openapi: 3.0.0
info:
  title: saver.php
  version: 1.0.0
paths:
  /{key}:
    get:
      summary: 读取资料
      parameters:
        - name: key
          in: path
          required: true
          schema:
            type: string
      responses:
        200:
          description: 读取资料
          content:
            application/json:
              schema:
                type: object
